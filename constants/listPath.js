export const whiteList = Object.freeze([
    "/",
    "/login",
    "/register",
    "/profile",
    "/products",
    "/products/:id",
    "/products",
    "/products/update/:id",
  ]);
  
  export const blackList = Object.freeze([]);